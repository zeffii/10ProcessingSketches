class Rectangle{
 int x,y,w,h;

 Rectangle(float x, float y, float w, float h){
   this.x = int(x);
   this.y = int(y);
   this.w = int(w);
   this.h = int(h); 
  
 }
  
 Rectangle(){
   // arbitrary values.
   this.x = 0;
   this.y = 0;
   this.w = 0;
   this.h = 0; 
  
 }
  
  
  
  
}
